import os

files = os.listdir"."
for f in files:
    with openf, "r" as infile, openf + "-out.txt", "w" as outfile:
        for line in infile:
            outfile.writeline.replace"", ""
                              .replace"", ""
                              .replace""", """
